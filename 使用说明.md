# Nocturne · 夜曲钢琴

双击本文件夹中的「启动夜曲钢琴」快捷方式即可使用。程序、源码、设置和示范曲全部保存在这个文件夹内。

## 开始演奏

- 启动后会自动连接或启动 `D:\Music\Modartt\Pianoteq 9\Pianoteq 9.exe`。右上角显示「PIANOTEQ 已连接」即可演奏。
- 鼠标点击琴键，或按电脑键盘。默认 A = C4（中央 C），白键为 A S D F G H J K L ; ' /，黑键分布在 W E T Y U O P ]。
- 可以同时按多个键弹和弦。普通电脑键盘同时识别的按键数量取决于键盘硬件。
- 按住 Space 延音，松开结束；也可点击「延音」切换。↑ / ↓ 或八度按钮可上下移调两组八度。
- Esc 立即停止播放并止音。窗口失焦会释放手动按住的音符和延音；MIDI 自动播放继续。
- 「触键力度」控制电脑键盘和鼠标演奏的力度，MIDI 文件保留自身力度。「输出音量」调整 Pianoteq 音量。
- 音色下拉框列出 Pianoteq 返回的已授权音色。

## 自定义键位

打开「设置与按键映射」，点击「点击后按下一个键」，按下想用的键，选择音符，再点击「绑定 / 更新」，最后「保存设置」。

支持字母、数字、常用标点、数字小键盘；Space、↑、↓、Esc 是保留键。映射音域为 A0–C8（MIDI 21–108）；界面显示 C2–C7，范围外的音符仍会发声。多枚按键可以绑定同一个音符。可删除映射、恢复默认、导入/导出 JSON。

设置保存在项目根目录的 `settings.json`。改变 Pianoteq 路径后点击「连接音源」；若旧音源服务仍在运行，程序会优先复用它，关闭旧服务后再连接才会启动新路径。

## MIDI 自动演奏

点击「打开 MIDI」或将 `.mid` / `.midi` 文件拖进窗口。按「播放」自动演奏，可暂停、停止、拖动进度条、勾选循环。附带原创示范曲「First Light」。启动时载入示范曲但不自动播放。

播放由 Pianoteq 原生 MIDI 播放器负责；程序绘制音符瀑布和当前按下的琴键。支持标准 MIDI Format 0 / 1、多个音轨、速度变化和 SMPTE 时间格式；Format 2 需先转换为 0 / 1。单文件上限 32 MB。可视化表示音符按下/松开，不延长显示踏板造成的尾音。MIDI 中的 Program Change 等事件会按 Pianoteq 自身设置解释。

## 音源与音频

程序通过仅绑定本机的 `127.0.0.1:18981` JSON-RPC 接口连接 Pianoteq 9，不需要虚拟 MIDI 驱动。手动演奏也通过此本机接口传送，实际响应速度受系统负载、音频缓冲和声卡影响。

音频输出设备、驱动和缓冲大小在 Pianoteq 自身界面设置。若已连接却没声音，请检查 Pianoteq 的音频设备和系统音量。若提示断开，确认 Pianoteq 已运行，然后点击「连接音源」。已有不带控制接口的 Pianoteq 窗口时，可能另外打开一个用于连接的实例。

关闭夜曲钢琴会停止本程序使用的 MIDI 播放和音符，Pianoteq 窗口保留。重复打开夜曲钢琴会回到现有窗口。

## 项目结构与开发

- `App/Nocturne.exe`：编译好的 Windows 桌面程序。
- `src/`：完整 C# / WPF 源码，无第三方 NuGet 依赖。
- `Music/First Light.mid`：原创示范曲（可自由修改和使用）。
- `settings.json`：键位、力度和移调设置。
- `build.cmd`：重新编译（需 .NET 8 SDK）。
- `test.cmd`：MIDI 解析与配置测试；`test.cmd --integration` 加做真实音源测试。

当前电脑已安装 .NET 8 桌面运行时，可直接运行。拷贝到其他电脑时需要安装 .NET 8 Desktop Runtime，并在设置中指定那台电脑上的 Pianoteq 程序。

开发验证：MIDI 速度变化、跨轨合并、running status、零力度 Note Off、损坏文件处理；真实音源连接、音符/踏板、原生 MIDI 播放/暂停/跳转；界面按键和映射保存。集成测试会播放短音符并切换音源中的 MIDI 文件，测试前请关闭夜曲钢琴主界面。

接口依据：Pianoteq 9.0.1 自带的 `/jsonrpc` 文档。
